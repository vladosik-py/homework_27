[tool.poetry]
name = "djangoproject"
version = "0.1.0"
description = ""
authors = ["vladosik_py <vbsmulskaya@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
